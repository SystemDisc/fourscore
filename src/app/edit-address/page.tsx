import { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'FourScore - Edit Address',
  description: 'Edit your address to ensure accurate candidate matching.',
};

export default async function Page() {
  return null;
}
